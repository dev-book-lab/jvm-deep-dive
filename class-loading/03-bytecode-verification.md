# Bytecode Verification - 바이트코드 검증

---

## 🎯 핵심 질문

이 문서를 읽고 나면 다음 질문에 답할 수 있습니다.

- JVM은 왜 실행 전에 바이트코드를 검증하는가? 컴파일러가 이미 타입을 체크하지 않았나?
- Verifier가 없으면 어떤 공격이 가능한가?
- Data Flow Analysis는 어떻게 "스택 오버플로우가 없음"을 실행 전에 증명하는가?
- `-Xverify:none`은 무엇을 포기하는 것이며, 언제 쓸 수 있는가?
- StackMapTable은 무엇이고 왜 Java 7에서 도입됐는가?

---

## 🔍 왜 이게 존재하는가

### 문제: JVM은 .class 파일을 신뢰할 수 없다

```java
// 개발자가 작성한 Java 코드
String s = "hello";
int len = s.length();  // 컴파일러가 타입 안전성 보장
```

Java 컴파일러(`javac`)는 타입을 검사하고 안전한 바이트코드를 생성한다.  
하지만 JVM이 실행하는 `.class` 파일은 반드시 `javac`가 만든 것이 아닐 수 있다.

```
.class 파일의 출처:
  ① javac가 정상 컴파일한 파일       → 안전
  ② 다른 JVM 언어 컴파일러 (Kotlin, Scala) → 대부분 안전
  ③ 바이트코드 조작 도구 (ASM, Javassist) → 버그 가능
  ④ 네트워크에서 다운로드한 파일     → 악의적일 수 있음
  ⑤ 헥스 에디터로 직접 수정한 파일  → 의도적 공격 가능
  ⑥ 오래된 컴파일러의 버그로 생성   → 실수 가능
```

Verifier가 없다면:

```
공격 시나리오:
  바이트코드를 조작해 String 타입 변수에 Integer 인스턴스를 넣음
  JVM이 String 메서드를 호출할 때 잘못된 메모리 위치 접근
  → JVM 크래시 또는 임의 메모리 읽기/쓰기

  또는:
  operand stack 언더플로우를 이용해 스택 아래 메모리 접근
  → 다른 스레드의 스택 프레임 오염
  → 권한 상승 공격 가능
```

**Verifier의 역할:** `.class` 파일의 출처와 무관하게, 실행 전에 안전성을 수학적으로 증명한다.

---

## 📐 내부 구조

### 1. 검증 단계 구성

검증은 4개의 Pass로 나뉜다.

```
Pass 1: 파일 형식 검사    ← Loading 단계에서 실행
Pass 2: 구조적 제약 검사  ← Linking(Verification) 단계에서 실행
Pass 3: Data Flow Analysis
Pass 4: 심볼릭 참조 검증  ← Resolution 단계에서 실행 (지연 가능)
```

---

### 2. Pass 1 — 파일 형식 검사

`.class` 파일의 기본 구조가 JVM 명세와 일치하는지 확인한다.

```
.class 파일 구조:
┌────────────────────────────────┐
│ magic: 0xCAFEBABE (4 bytes)    │ ← 이것이 아니면 즉시 거부
├────────────────────────────────┤
│ minor_version (2 bytes)        │
│ major_version (2 bytes)        │ ← JVM 버전 범위 확인
├────────────────────────────────┤
│ constant_pool_count (2 bytes)  │
│ constant_pool[] (가변)          │ ← 상수 풀 태그 유효성 확인
├────────────────────────────────┤
│ access_flags (2 bytes)         │ ← 유효한 플래그 조합인가?
│ this_class (2 bytes)           │
│ super_class (2 bytes)          │
├────────────────────────────────┤
│ interfaces_count (2 bytes)     │
│ interfaces[] (가변)             │
├────────────────────────────────┤
│ fields_count (2 bytes)         │
│ fields[] (가변)                 │ ← 필드 디스크립터 형식 확인
├────────────────────────────────┤
│ methods_count (2 bytes)        │
│ methods[] (가변)                │ ← 메서드 시그니처 형식 확인
├────────────────────────────────┤
│ attributes_count (2 bytes)     │
│ attributes[] (가변)             │
└────────────────────────────────┘
```

```bash
# 실제 .class 파일의 magic number 확인
xxd MyClass.class | head -1
# 00000000: cafe babe 0000 003d 00xx ...
#           ^^^^^^^^^^             ← CAFEBABE 확인
#                     ^^^^ ^^^^    ← Java 21 = version 65 = 0x0041
```

---

### 3. Pass 2 — 구조적 제약 검사

클래스 구조의 의미론적 제약을 확인한다.

```
검사 항목:
  ① 클래스 계층 제약
     - final 클래스를 상속하지 않았는가?
     - 인터페이스가 Object 이외의 클래스를 상속하지 않았는가?
     - 클래스(Object 제외)에 super class가 있는가?
     - 인터페이스 메서드가 native 또는 synchronized가 아닌가?

  ② 상수 풀 참조 유효성
     - 모든 상수 풀 인덱스가 범위 내인가?
     - 참조 타입이 실제 존재하는 엔트리인가?

  ③ 필드/메서드 제약
     - 메서드 이름이 <init>/<clinit>이 아닌 '<' 또는 '>'를 포함하지 않는가?
     - final 필드가 두 번 할당되지 않는가?
     - 접근 제어자 조합이 유효한가? (private + abstract 같은 불가능한 조합)
```

---

### 4. Pass 3 — Data Flow Analysis (핵심)

바이트코드의 실행 흐름을 정적 분석해 타입 안전성을 증명한다.  
이것이 Verifier의 핵심이자 가장 복잡한 부분이다.

#### 기본 원리

```
각 바이트코드 명령어는 실행 전/후의 타입 상태가 정해져 있다.

예: iadd (int 덧셈)
  실행 전 operand stack: [..., int, int]
  실행 후 operand stack: [..., int]

Verifier는 모든 실행 경로에서
각 명령어 실행 시점의 스택 상태를 계산한다.
예측된 타입과 실제 명령어의 기대 타입이 불일치하면 → VerifyError
```

```
구체적인 검사 예시:

바이트코드:
  iload_1        // 지역 변수 1번 (int)을 스택에 push
  iload_2        // 지역 변수 2번 (int)을 스택에 push
  iadd           // int 두 개를 pop하고 합계를 push
  istore_3       // 결과를 지역 변수 3번에 저장

Verifier의 타입 추적:
  명령어 전      명령어       명령어 후
  []          iload_1     → [int]
  [int]       iload_2     → [int, int]
  [int, int]  iadd        → [int]
  [int]       istore_3    → []
  → 모든 타입 일치. 검증 통과.

오류 케이스:
  []          iload_1     → [int]
  [int]       dload_2     → [int, double]  // double은 2슬롯
  [int, double, double]  iadd  → ???
  // iadd은 스택 상위 2개가 int여야 하는데 double이 있음
  // → VerifyError: Bad type on operand stack
```

#### 분기(branch)가 있는 경우 — Join Point 처리

```java
// Java 코드
int result;
if (condition) {
    result = 1;      // int
} else {
    result = 2.0;    // double (타입 불일치 예시)
}
return result;
```

```
바이트코드 흐름:
              [시작]
                │
         ifeq   │  분기
        ┌───────┴───────┐
    true│               │false
        │               │
  iconst_1          dconst_2
  istore result     dstore result
        │               │
        └───────┬───────┘
                │ Join Point
            iload result
              ireturn

Join Point에서 Verifier의 타입 추론:
  true 경로: result = int
  false 경로: result = double
  Join: int ∩ double = ???
  → 타입이 일치하지 않음 → VerifyError
```

#### StackMapTable — Java 7에서 도입된 이유

Pass 3의 Data Flow Analysis는 계산 비용이 크다.  
특히 분기가 많은 메서드에서 JVM이 직접 타입 추론을 해야 해서 로딩 시간 증가.

```
Java 7 이전 (Type Inference Verifier):
  JVM이 모든 분기를 따라가며 타입 상태를 직접 계산
  분기가 100개면 최대 2^100 경로 탐색 (이론상)
  → 역 직렬화 공격, 악의적 클래스로 JVM 지연 가능

Java 7 이후 (Type Checking Verifier):
  컴파일러가 각 분기 합류점(Join Point)에서의 타입 상태를
  StackMapTable attribute로 미리 계산해 .class 파일에 포함
  JVM은 이미 답이 있는 표를 확인하기만 하면 됨
  → O(n) 선형 시간으로 검증 완료
```

```
StackMapTable 구조 (javap -verbose로 확인):

StackMapTable: number_of_entries = 2
  frame_type = 255 (full_frame)
    offset_delta = 14
    locals = [int, int]
    stack = []
  frame_type = 64 (same_locals_1_stack_item)
    offset_delta = 7
    stack = [int]

→ 분기 합류점에서의 정확한 타입 상태를 미리 명시
```

---

### 5. Pass 4 — 심볼릭 참조 검증

Resolution 단계에서 심볼릭 참조를 실제 클래스/메서드/필드로 해결할 때 수행된다.

```
검증 항목:
  ① 참조한 클래스가 실제로 존재하는가?
  ② 참조한 메서드/필드가 해당 클래스에 존재하는가?
  ③ 접근 권한이 있는가? (private, protected, package-private)
  ④ 메서드 시그니처가 일치하는가?
```

```java
// Pass 4가 실패하는 예시

// A.class가 B의 private 메서드를 호출하도록 바이트코드 조작
// → IllegalAccessError

// A.class가 존재하지 않는 메서드를 호출하도록 조작
// → NoSuchMethodError

// A.class가 존재하지 않는 클래스를 참조하도록 조작
// → NoClassDefFoundError
```

---

## 💻 실험으로 확인하기

### 실험 1: StackMapTable 직접 확인

```java
// StackMapDemo.java
public class StackMapDemo {
    public int compute(boolean flag) {
        int result;
        if (flag) {
            result = 1;
        } else {
            result = 2;
        }
        return result;  // ← 이 Join Point에서 StackMapTable 필요
    }
}
```

```bash
javac StackMapDemo.java
javap -verbose StackMapDemo.class

# 출력에서 StackMapTable 확인:
# StackMapTable: number_of_entries = 2
#   frame_type = ...
```

---

### 실험 2: VerifyError 직접 발생시키기

ASM 라이브러리로 의도적으로 잘못된 바이트코드 생성:

```java
// 의존성: org.ow2.asm:asm:9.x
import org.objectweb.asm.*;

public class GenerateBadBytecode {
    public static void main(String[] args) throws Exception {
        ClassWriter cw = new ClassWriter(ClassWriter.COMPUTE_FRAMES);
        cw.visit(Opcodes.V17, Opcodes.ACC_PUBLIC,
                "BadClass", null, "java/lang/Object", null);

        MethodVisitor mv = cw.visitMethod(
            Opcodes.ACC_PUBLIC | Opcodes.ACC_STATIC,
            "badMethod", "()V", null, null);
        mv.visitCode();

        // ❌ 잘못된 바이트코드: int를 push하고 String 메서드 호출
        mv.visitLdcInsn(42);                // int 42 push
        mv.visitMethodInsn(                  // String.length() 호출 (타입 불일치!)
            Opcodes.INVOKEVIRTUAL,
            "java/lang/String",
            "length", "()I", false);
        mv.visitInsn(Opcodes.RETURN);
        mv.visitMaxs(2, 0);
        mv.visitEnd();
        cw.visitEnd();

        byte[] bytecode = cw.toByteArray();

        // 직접 defineClass로 로드
        ClassLoader loader = new ClassLoader() {
            public Class<?> define(byte[] b) {
                return defineClass("BadClass", b, 0, b.length);
            }
        };

        try {
            Class<?> clazz = ((Object) loader).getClass()
                .getMethod("define", byte[].class)
                .invoke(loader, (Object) bytecode);
        } catch (java.lang.reflect.InvocationTargetException e) {
            System.out.println("예상된 에러: " + e.getCause());
            // java.lang.VerifyError: Bad type on operand stack
        }
    }
}
```

---

### 실험 3: -Xverify 옵션별 동작 비교

```bash
# 기본 검증 (권장)
java MyApp

# 원격 클래스만 검증 건너뜀 (Java 8 이하 일부 옵션)
java -Xverify:remote MyApp

# 모든 검증 비활성화 (매우 위험)
java -Xverify:none MyApp

# 시작 시간 비교 (대규모 앱에서 측정)
time java -cp large-app.jar com.example.Main
time java -Xverify:none -cp large-app.jar com.example.Main
# 차이는 클래스 수에 비례 (수천 개 클래스면 수백ms 차이)
```

```bash
# StackMapTable이 없는 Java 6 이하 클래스 로드 시
java --class-path legacy-java6.jar MyApp
# 경고 또는 VerifyError 가능
# -XX:-FailOverToOldVerifier 로 동작 변경 가능 (Java 8 이하)
```

---

### 실험 4: javap로 검증 관련 정보 분석

```bash
# 자세한 바이트코드 출력 (StackMapTable 포함)
javap -c -verbose MyClass.class

# Code 섹션에서 주목할 부분:
# stack=2, locals=3, args_size=1    ← Verifier가 검사하는 max 값
#   0: iload_1
#   1: iload_2
#   2: iadd
#   3: ireturn
# StackMapTable: number_of_entries = 0  ← 분기 없으면 비어 있음
```

---

## ⚡ 실무 임팩트

### VerifyError가 발생하는 실제 상황

```
상황 1: 바이트코드 조작 라이브러리 버그
  Lombok, Mockito, JaCoCo, AspectJ 등이
  바이트코드를 변환할 때 버그가 있으면 VerifyError 발생
  
  에러 예시:
  java.lang.VerifyError: Bad local variable type
    Exception Details:
      Location: com/example/Service.process(...)
      Reason: Type 'java/lang/Object' is not assignable to 'java/lang/String'
  
  진단:
  java -verbose:class MyApp | grep "Loaded"
  → 어떤 클래스가 어느 ClassLoader에서 로드됐는지 확인
  → 바이트코드 조작 에이전트가 연루된 클래스 식별

상황 2: Java 버전 불일치
  Java 17로 컴파일된 클래스를 Java 11 JVM에서 실행
  → UnsupportedClassVersionError (Verify의 Pass 1에서 탐지)
  
  에러 예시:
  java.lang.UnsupportedClassVersionError: MyClass has been
  compiled by a more recent version of the Java Runtime (61.0),
  this version of the Java Runtime only recognizes class
  file versions up to 55.0

  버전 대응표:
  Java 8  → major version 52
  Java 11 → major version 55
  Java 17 → major version 61
  Java 21 → major version 65

상황 3: 오래된 라이브러리 (Java 6 이하로 컴파일)
  StackMapTable이 없는 클래스를 Java 7+ JVM에서 로드
  기본적으로는 구형 Verifier(Type Inference)로 폴백하지만
  특정 조건에서 VerifyError 발생 가능
```

### -Xverify:none 사용 판단 기준

```
사용해도 되는 경우:
  ✅ 완전히 통제된 환경 (내부 빌드 서버, CI/CD)
  ✅ 신뢰된 소스에서만 클래스 로드
  ✅ 기동 시간이 극도로 중요한 배치 작업
  ✅ 이미 검증된 이미지를 반복 실행하는 컨테이너 환경

사용하면 안 되는 경우:
  ❌ 외부에서 클래스/플러그인을 로드하는 애플리케이션
  ❌ 프로덕션 서버 (보안 취약점)
  ❌ 신뢰할 수 없는 클래스패스가 포함된 경우
  ❌ Java 에이전트나 바이트코드 변환이 있는 경우

Java 13+ 변화:
  -Xverify:none 과 -noverify 는 deprecated
  GraalVM Native Image로 컴파일 시 AOT 단계에서 검증 완료
  → 런타임 검증 불필요 (다른 방식의 안전성 보장)
```

---

## 🚫 흔한 오해

### "컴파일러가 타입을 체크했으니 검증은 불필요하다"

```
❌ 잘못된 이해:
  javac가 타입 안전성을 보장했으므로
  바이트코드 검증은 중복이다.

✅ 실제:
  JVM은 .class 파일이 javac에서 왔다는 것을 알 수 없다.
  JVM 명세를 구현하는 언어는 Java 외에도
  Kotlin, Scala, Groovy, Clojure 등 수십 가지.
  바이트코드 조작 도구로 언제든 .class를 수정 가능.
  Verifier는 출처와 무관하게 안전성을 보장하는 마지막 방어선.
```

### "VerifyError는 항상 내 코드의 버그다"

```
❌ 잘못된 이해:
  VerifyError가 발생하면 내가 작성한 코드에 문제가 있다.

✅ 실제:
  내가 직접 작성한 Java 코드에서 VerifyError가 발생하는 경우는 거의 없다.
  대부분의 원인:
  
  1. 바이트코드 조작 라이브러리의 버그 (Lombok, MockK, JaCoCo 등)
  2. 서로 다른 Java 버전으로 컴파일된 클래스 혼용
  3. 의도적인 바이트코드 조작 (해킹 시도)
  4. 일부 오래된 AOT/JIT 최적화 도구의 버그
  
  따라서 VerifyError 발생 시 먼저 빌드 도구와 라이브러리 버전을 점검하라.
```

### "StackMapTable은 성능 최적화를 위한 것이다"

```
❌ 잘못된 이해:
  StackMapTable은 JVM 실행 속도를 높이기 위해 추가됐다.

✅ 실제:
  StackMapTable은 검증 속도를 높이기 위한 것이다.
  (JVM 실행 속도와는 직접 관련 없음)
  
  Java 6 이전의 Type Inference Verifier는 분기 경로를
  반복해서 분석하는 구조라 악의적인 클래스로 JVM을
  의도적으로 느리게 만드는 DoS 공격에 취약했다.
  
  StackMapTable은 컴파일러가 분석 결과를 미리 제공해
  검증을 O(n) 시간으로 제한한다. 보안 개선이 주목적.
```

---

## 📌 핵심 정리

```
Verifier가 존재하는 이유
  JVM은 .class 파일의 출처를 신뢰할 수 없음
  실행 전에 수학적으로 안전성을 증명
  타입 오염, 스택 손상, 권한 우회 공격 차단

4개의 Pass
  Pass 1: magic number, 버전, 파일 형식 확인
  Pass 2: final 상속, 접근자 조합 등 구조적 제약
  Pass 3: Data Flow Analysis — 타입 안전성 정적 증명 (핵심)
  Pass 4: 심볼릭 참조 실제 존재 여부 및 접근 권한 확인

Pass 3의 핵심 원리
  모든 실행 경로에서 각 명령어 시점의 스택/변수 타입 추론
  명령어 기대 타입과 추론 타입 불일치 → VerifyError

StackMapTable (Java 7+)
  컴파일러가 분기 합류점의 타입 상태를 미리 계산해 포함
  JVM이 직접 추론할 필요 없음 → O(n) 검증
  보안 목적 (DoS 방어) + 성능 부수효과

VerifyError 실제 원인
  내 코드 버그보다는 바이트코드 조작 라이브러리 버그,
  Java 버전 불일치, 의도적 공격이 대부분

-Xverify:none
  Java 13+ deprecated
  완전히 통제된 환경에서만 제한적으로 사용
  프로덕션에서는 절대 사용 금지
```

---

## 🤔 생각해볼 문제

**Q1.** Java 언어에는 없는 기능(예: 타입 캐스팅 없이 int를 String으로 직접 대입)을 바이트코드 수준에서 구현하려 한다면, Verifier의 어떤 Pass에서 걸리는가? 그 이유는?

**Q2.** StackMapTable이 없는 Java 6로 컴파일된 라이브러리를 Java 17 JVM에서 사용할 때 어떤 일이 발생하는가? JVM은 이를 어떻게 처리하는가?

**Q3.** CI/CD 파이프라인에서 기동 시간을 줄이기 위해 `-Xverify:none`을 프로덕션 서버에 적용하려 한다. 이것의 위험성을 구체적인 공격 시나리오로 설명하고, 대안을 제시하라.

> 💡 **해설**
>
> **Q1.** Pass 3(Data Flow Analysis)에서 걸린다. Verifier가 각 명령어 시점의 스택 타입을 추론하는데, `istore`(int 저장)를 String 타입 변수 슬롯에 적용하면 타입 불일치를 탐지한다. Pass 2는 구조적 제약(상속, 접근자)을 보고, Pass 4는 참조 존재 여부를 보기 때문에, 실제 타입 흐름 분석은 Pass 3의 몫이다.
>
> **Q2.** Java 7+의 기본 Verifier(Type Checking)는 StackMapTable을 요구한다. StackMapTable이 없는 클래스를 만나면 레거시 Verifier(Type Inference)로 폴백을 시도한다. Java 8까지는 이 폴백이 기본 동작이었지만, 이후 버전에서는 폴백이 deprecated되거나 제거됐다. Java 17에서는 StackMapTable 없는 클래스를 로드하면 VerifyError가 발생할 수 있다. 해결책은 해당 라이브러리를 최신 버전으로 교체하거나, 정말 불가피하면 `-XX:-FailOverToOldVerifier`(지원하는 버전에서)를 쓰는 것.
>
> **Q3.** 위험 시나리오: 플러그인이나 외부 jar를 동적으로 로드하는 경우, 공격자가 조작된 .class 파일을 해당 경로에 심을 수 있다. `-Xverify:none`이면 타입 오염 코드가 그대로 실행되어 JVM 메모리를 직접 조작 가능 → 권한 상승 또는 데이터 탈취. 대안: (1) GraalVM Native Image로 AOT 컴파일 — 빌드 타임에 검증 완료, 런타임 검증 불필요하면서 기동 시간도 대폭 단축. (2) CDS(Class Data Sharing)로 검증된 클래스를 아카이브 — 재검증 없이 재사용. (3) 기동 시간 문제라면 검증 비활성화보다 컨테이너 스냅샷(예: AWS Lambda SnapStart) 활용.

---

## 📚 참고 자료

- [JVMS §4 — The class File Format](https://docs.oracle.com/javase/specs/jvms/se21/html/jvms-4.html)
- [JVMS §4.10 — Verification of class Files](https://docs.oracle.com/javase/specs/jvms/se21/html/jvms-4.html#jvms-4.10)
- [JEP 351 — ZGC: Uncommit Unused Memory](https://openjdk.org/jeps/351) *(StackMapTable 관련 JEP 139 참고)*
- [ASM Library](https://asm.ow2.io/) — 바이트코드 조작 실험용

---

<div align="center">

**[⬅️ 이전: Loading → Linking → Initializing](./02-loading-linking-initializing.md)** | **[다음: Symbolic Reference Resolution ➡️](./04-symbolic-reference-resolution.md)**

</div>
